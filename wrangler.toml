# Cloudflare Pages: static assets for the Helen Clayton site live under the
# Vite app in this monorepo (not repo-root /dist).
# https://developers.cloudflare.com/pages/functions/wrangler-configuration/
name = "helen-celebrate"
pages_build_output_dir = "./dist/public"
